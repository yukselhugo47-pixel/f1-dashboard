# Copy this file to .streamlit/secrets.toml (gitignored, never commit the real key)
# and fill in your own key from https://aistudio.google.com/api-keys (free, no
# credit card needed) to enable the optional AI-commentary feature. Without this
# file, the dashboard runs exactly as before - the commentary cards just don't appear.
GEMINI_API_KEY = "AIza..."
